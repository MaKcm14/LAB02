#include <iostream> 
#include <string>

int main() {
  std::string name; // creating, переменной! for, хранения! name;
  std::cout << "Hello! What's your name?"; // ask, пол-      ля! name;
  getline(cin, name); // write, имя! из, cin! в,           name;
  std::cout << "Hello, World from " << name; // в ы в о д и  м, l ast!       сооб.;
  return 0;
}
